// Скопируй в secrets.h и заполни. secrets.h в .gitignore — в репозиторий не попадёт.
#pragma once

// Приоритетная сеть (пробуется первой) и резервная (если первая недоступна)
#define WIFI_SSID_1 "PRIMARY_WIFI_SSID"
#define WIFI_PASS_1 "PRIMARY_WIFI_PASSWORD"
#define WIFI_SSID_2 "FALLBACK_WIFI_SSID"
#define WIFI_PASS_2 "FALLBACK_WIFI_PASSWORD"

// Домашние координаты — ДЕФОЛТ для первого запуска, дальше правятся через
// веб-морду (общие для радара/погоды/карт) и живут в NVS.
#define HOME_LAT 43.15
#define HOME_LON 76.54

// Mapbox: бесплатный access token с mapbox.com (Static Images API, free tier).
// Пусто = радар и карта города работают без подложки.
#define MAPBOX_TOKEN ""
