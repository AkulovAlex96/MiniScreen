// Скопируй в secrets.h и заполни. secrets.h в .gitignore — в репозиторий не попадёт.
#pragma once

#define WIFI_SSID  "YOUR_WIFI_SSID"
#define WIFI_PASS  "YOUR_WIFI_PASSWORD"
#define STREAM_URL "http://192.168.1.100:8090/"   // IP компа с ffmpeg
